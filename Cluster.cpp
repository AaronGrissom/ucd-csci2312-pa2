# include "Cluster.h"
# include <iostream>

using namespace std;
using namespace Clustering;

/*
    Cluster::Cluster()
    {
        
    }

    // The big three: cpy ctor, overloaded operator=, dtor
    Cluster::Cluster(const Cluster &x)
    {
        
    }
    Cluster &operator=(const Cluster &x)
    {
        
    }
    Cluster::~Cluster()
    {
        
    }

    // Getters/setters
    int Cluster::getSize() const // TODO add to the requirements
    {
        
    }

    // Set functions: They allow calling c1.add(c2.remove(p));
    void Cluster::add(const Point &x) // TODO add asc order to the requirements
    {
        
    }
    const Point &remove(const Point &x)
    {
        
    }
    bool Cluster::contains(const Point &x)
    {
        
    }

    // Overloaded operators

    // Members: Subscript
    const Point &operator[](unsigned int index) const // notice: const
    {
        
    }

    // Members: Compound assignment (Point argument)
    Cluster &operator+=(const Point &x)
    {
        
    }
    Cluster &operator-=(const Point &x)
    {
        
    }

    // Members: Compound assignment (Cluster argument)
    Cluster &operator+=(const Cluster &x) // union
    {
            
    }
    Cluster &operator-=(const Cluster &x) // (asymmetric) difference
    {
            
    }

    // Friends: IO
    std::ostream &operator<<(std::ostream &x, const Cluster &y)
    {
            
    }
    std::istream &operator>>(std::istream &x, Cluster &y)
    {
            
    }

    // Friends: Comparison
    bool operator==(const Cluster &x, const Cluster &y)
    {
            
    }
    bool operator!=(const Cluster &x, const Cluster &y)
    {
        
    }

    // Friends: Arithmetic (Cluster and Point)
    const Cluster operator+(const Cluster &x, const Point &y)
    {
        
    }
    const Cluster operator-(const Cluster &x, const Point &y)
    {
        
    }

    // Friends: Arithmetic (two Clusters)
    const Cluster operator+(const Cluster &x, const Cluster &y) // union
    {
            
    }
    const Cluster operator-(const Cluster &x, const Cluster &y) // (asymmetric) difference
    {
        
    }
*/